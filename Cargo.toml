[package]
name = "lau-async-tick"
version = "0.1.0"
edition = "2021"
description = "THE async tick engine — non-blocking tick processing with priority queues"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
